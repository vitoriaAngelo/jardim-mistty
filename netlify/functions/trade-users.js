const SUPABASE_URL='https://luvjridqxqpxnljucnur.supabase.co';const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;const {authenticateTwitch}=require('./_auth');
exports.handler=async event=>{const headers={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization, X-Garden-Session','Access-Control-Allow-Methods':'GET, OPTIONS','Content-Type':'application/json'};if(event.httpMethod==='OPTIONS')return{statusCode:200,headers,body:''};try{await authenticateTwitch(event)}catch{return{statusCode:401,headers,body:JSON.stringify({error:'Login necessário'})}}try{const g=await fetch(`${SUPABASE_URL}/rest/v1/gardens?select=username&limit=500`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`}});const users=[...new Set((await g.json()).map(x=>x.username).filter(Boolean))];return{statusCode:200,headers,body:JSON.stringify({users})}}catch(error){return{statusCode:500,headers,body:JSON.stringify({error:error.message})}}};
