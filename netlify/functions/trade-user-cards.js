const U='https://luvjridqxqpxnljucnur.supabase.co',K=process.env.SUPABASE_SERVICE_ROLE_KEY;const {authenticateTwitch}=require('./_auth');exports.handler=async e=>{const h={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization, X-Garden-Session','Access-Control-Allow-Methods':'GET, OPTIONS','Content-Type':'application/json'};if(e.httpMethod==='OPTIONS')return{statusCode:200,headers:h,body:''};try{await authenticateTwitch(e)}catch{return{statusCode:401,headers:h,body:JSON.stringify({error:'Login necessário'})}}const n=(e.queryStringParameters?.username||'').replace(/^@/,'');if(!n)return{statusCode:400,headers:h,body:'{}'};const r=await fetch(`${U}/rest/v1/player_cards?username=eq.${encodeURIComponent(n)}&quantity=gt.1&select=card_id,quantity`,{headers:{apikey:K,Authorization:`Bearer ${K}`}});return{statusCode:r.ok?200:500,headers:h,body:JSON.stringify({cards:r.ok?await r.json():[]})}};
