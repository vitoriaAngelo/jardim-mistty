const SUPABASE_URL='https://luvjridqxqpxnljucnur.supabase.co';
const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;
const {authenticateTwitch}=require('./_auth');
exports.handler=async event=>{const headers={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization, X-Garden-Session','Access-Control-Allow-Methods':'GET, OPTIONS','Content-Type':'application/json'};if(event.httpMethod==='OPTIONS')return{statusCode:200,headers,body:''};if(event.httpMethod!=='GET')return{statusCode:405,headers,body:JSON.stringify({error:'Method Not Allowed'})};try{await authenticateTwitch(event)}catch(error){return{statusCode:401,headers,body:JSON.stringify({error:'Login necessário'})}}const username=(event.queryStringParameters?.username||'').replace(/^@/,'').trim();if(!username)return{statusCode:400,headers,body:JSON.stringify({error:'username required'})};try{const response=await fetch(`${SUPABASE_URL}/rest/v1/player_cards?username=eq.${encodeURIComponent(username)}&quantity=gt.1&select=card_id,quantity`,{headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`}});if(!response.ok)return{statusCode:500,headers,body:JSON.stringify({error:'Não foi possível buscar as cartas'})};const rows=await response.json();return{statusCode:200,headers,body:JSON.stringify({username,cards:rows.map(row=>({card_id:row.card_id,quantity:Number(row.quantity)||0}))})}}catch(error){return{statusCode:500,headers,body:JSON.stringify({error:error.message})}}};
